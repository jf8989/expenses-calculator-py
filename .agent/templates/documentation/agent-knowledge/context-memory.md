# Project Context Memory

> [!IMPORTANT]
> **Purpose**: Highly curated repository of project-specific business rules, dependency quirks, or architectural invariants discovered during execution.
> **Usage**: Reference this during startup to load domain knowledge that is not obvious from the code structure alone.
> **Maintenance**: Update ONLY when a new, stable, and non-obvious piece of project knowledge is unearthed. Do NOT use for session logs or general status.

---

## Domain Logic & Invariants

- No project-specific invariants recorded yet.

## Repo Map

> Last updated by: nobody yet
> Format: `path — what it is — depends on / used by`. One line per key file or folder.
> Refresh at phase end. Update only the lines for files you touched; never delete another agent's line unless the file itself is gone. Keep it under ~40 lines so any agent orients by reading this instead of re-scanning the codebase.

- No repo map recorded yet.
