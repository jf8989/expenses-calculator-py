---
name: end-session
description: "Explicit-only closeout that synchronizes plan status, pulse, repo memory, operator notes, and evidence-based runtime friction."
---
<!-- GENERATED: kind=workflows id=end-session source=workflows/end-session.md -->

# End Session

This workflow is explicit-only.

Close a material session without turning continuity into history.

1. Inspect landed diff and validation. Use the scoped documentation path for routine phase/trace evidence, manual procedures, pulse, credentials, touched Repo Map, and closeout; never convert missing evidence into completion or reclassify truth.
2. If controlled truth changed through an authorized workflow, ensure one coherent DOC entry records reason, approval, files, IDs, moves, and validation. Routine operational updates create no log noise.
3. Report, but do not silently repair, accepted-final-plan, documentation-conformance, or routing discrepancies. Read operator notes and mention untriaged items concisely; never drain them. Distinguish implemented code, automated proof, fallback-only behavior, real-service validation, credential setup, manual evidence, and deployment readiness.
4. Read the plan-owned `IMP` and `PRE` tables plus traceability evidence. Record project and active-phase implementation and pre-production readiness in pulse with date/workflow/runtime and exact open gates. Do not re-estimate from tests, files, or optimism. If implementation decreases, record approved added scope or invalid prior credit; if pre-production decreases, record the expired or failed evidence.
5. Close with the fixed `init` debrief: `Implementation:`, `Pre-production:`, `Finished:`, `Open gates:`, `Next:`, and `Need from you:`. `Next:` repeats the pulse pointer; end-session does not invent another recommendation. Keep everything above it to a short account of what landed and was verified.
6. Perform no broad runtime review. Only when this chat contains concrete protocol friction—rule contradicted reality, wrong routing, missing/misloaded skill, repeated avoidable work—or Juanfra redirected the work, append one compact current-chat entry to `improve/system-self-assessment.md` without reading existing entries. Otherwise append nothing.
7. Use `### YYYY-MM-DD HH:mm UTC — <runtime>` with `Signature`, `Friction`, `Evidence`, `Suspected runtime surface`, `Occurrence`, and `Corrections`. Reuse a stable signature for the same failure; determine occurrence by counting that signature only. Record each operator redirect. Exclude ordinary product bugs, choices, ratings, and normal difficulty; model evidence belongs to `model-review`.
