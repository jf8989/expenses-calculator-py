---
name: scaffold
description: "Explicit-only creation of approved structure, configuration, adapters, placeholders, and stubs before business logic."
---
<!-- GENERATED: kind=workflows id=scaffold source=workflows/scaffold.md -->

# Scaffold

This workflow is explicit-only.

Create the approved structural foundation without product behavior.

1. Verify the active approved plan, phase, standardized lifecycle, and platform overlay.
2. Use the scoped documentation-mutation path. Create only planned directories, configuration, interfaces, adapters, environment examples/placeholders, wiring, and minimal compiling stubs; never reclassify product truth.
3. Preserve architecture/behavior. For missing credentials, keep the planned boundary/fallback, update the exact keyed `credentials.md` row and operator guidance, and migrate only missing older-file fields without reformatting unrelated content. Never store/request secrets or claim real-service validation from fallback behavior.
4. Run structural validation and inspect the diff. On the first landed implementation change append one `begin-execution` DOC entry, set `Plan State: executing`, and point `State Evidence` to it; do not repeat that event later.
5. Update mapped phase status, evidence, touched Repo Map, and pulse to landed truth. Leave `Lifecycle State: standardized` when scoped postflight passes; report operator setup/manual checks and wider drift without repairing it.
6. Do not fill stubs with speculative business logic.
7. Recommend explicit `create` for the same approved phase; never invoke it.
